{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Engine (LLDB)",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}",
            "args": [],
            "stopOnEntry": false,
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "CMake: Build"
        }
    ]
}
