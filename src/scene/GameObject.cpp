#include "GameObject.h"

// Note: There are no function definitions here because all GameObject
// methods (like createGameObject, getId, constructors, etc.)
// were defined directly within the class declaration in GameObject.h.